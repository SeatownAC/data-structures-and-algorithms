const LinkedList = require('../II_insertions.js');

describe('Single Linked List Module', ()=> {

  it('should create a new list with a head of value null', () => {

    let newList = new LinkedList;

    expect(newList.head).toBeNull();
  })

  it('for append, if linked-list is empty should make only node the head node', () => {

    let newList = new LinkedList;

    

    // append(val) {

    //     const newNode =newNode(val);
    
    //     if (!this.head) {
    //         this.head = newNode;
    //         return this;
    //     }
    
    //     var currentNode = this.head;
    
    //     while(currentNode.next !== null) {
    //         currentNode = currentNode.next;
    //     }
    
    //     currentNode.next = newNode;

  }

)
});